apiVersion: v1
kind: Pod
metadata:
 name: vaultwarden-migration-helper
spec:
 containers:
 - name: alpine
   image: alpine:latest
   command: ["sleep", "infinity"]
   volumeMounts:
   - name: data
     mountPath: /old/data
   - name: new
     mountPath: /new
 volumes:
 - name: data
   persistentVolumeClaim:
     claimName: vaultwarden-data
 - name: new
   nfs:
     server: hactar.lan
     path: /mnt/tank/server/vaultwarden
